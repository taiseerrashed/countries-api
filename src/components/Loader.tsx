import { BeatLoader } from "react-spinners"

const Loader = () => {
  return (
    <div className="h-screen flex justify-center items-center">
        <BeatLoader color="#095a45"/>
    </div>
  );
};

export default Loader;